<odoo>

    <!-- ACTION -->
    <!-- <record id="action_project_profit" model="ir.actions.act_window">
        <field name="name">Profit &amp; Loss</field>
        <field name="res_model">project.project</field>
        <field name="view_mode">list,form</field> -->

        <!-- IMPORTANT: attach custom views -->
        <!-- <field name="view_ids" eval="[
            (5, 0, 0),
            (0, 0, {'view_mode': 'list', 'view_id': ref('view_project_profit_list')}),
            (0, 0, {'view_mode': 'form', 'view_id': ref('view_project_profit_form')})
        ]"/> -->
    <!-- </record> -->

    <record id="action_project_profit" model="ir.actions.act_window">
        <field name="name">Profit &amp; Loss</field>
        <field name="res_model">project.project</field>
        <field name="view_mode">list,form</field>

        <field name="view_id" ref="view_project_profit_list"/>

        <!-- FIX: replace view_ids with views -->
        <field name="views" eval="[
            (ref('view_project_profit_list'), 'list'),
            (ref('view_project_profit_form'), 'form')
        ]"/>
    </record>

    <!-- MENU -->
    <menuitem id="menu_project_profit"
              name="Profit &amp; Loss"
              parent="project.menu_project_report"
              action="action_project_profit"
              sequence="20"/>

</odoo>